/* eslint-disable @typescript-eslint/no-explicit-any */
// Simple Graph Data - just returns nodes and links as-is
export function createSimpleGraph(nodes: any[], links: any[]) {
  return { nodes, links };
}
